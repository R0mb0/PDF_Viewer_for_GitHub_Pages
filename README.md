# PDF_Viewer_for_GitHub_Pages-
A lightweight static PDF viewer page designed for GitHub Pages. It embeds a PDF using the browser’s native viewer when available and provides reliable fallback actions (open in a new tab / download) for browsers that don’t support inline PDF preview, especially on mobile.
